plugins {
	id 'java'
	id 'org.springframework.boot' version '{version}'
}

sourceCompatibility = '11'
targetCompatibility = '11'

bootBuildImage {
	publish = true
	docker {
		publishRegistry {
			username = "user"
			password = "secret"
		}
	}
}